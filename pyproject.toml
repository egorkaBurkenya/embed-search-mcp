[project]
name = "embed-search-mcp"
version = "0.1.0"
description = "MCP server for semantic code search via embed-server"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.0.0",
    "httpx>=0.27.0",
]

[project.scripts]
embed-search-mcp = "server:mcp.run"
